{

    # I have separated the scripts and the configuration, as having the
    # scripts in the `let` of the main file would make it long.

    imports = [

      ./lf.nix
      ./lf_scripts.nix

    ];

}
