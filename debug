I need help with an option i have defined as null or string type but nix tells me it's an attribute set

This is the file
```
{ config, lib, ... }:
with lib; with types;
let
    cfg = config.meta.disk;
in {
    options.meta.disk.filesystem = {
        type = nullOr str;
        default = null;
        description = "Filesystem to be used.";
    };

    imports = [
        ./disko/btrfs.nix
    ];

    config = {
        assertions = [
            {
                assertion = cfg.filesystem == null;
                message = "You must specify a filesystem for disko.";
            }
        ];
    };
}
```

I have defined in flake/hosts/raider/disk.nix as
```
meta.disk.filesystem = "btrfs";
```

The error is:
```
         at /nix/store/j8pbrsb3nybdap3hhg9kw0ffqd4rlbx6-source/lib/modules.nix:273:9:

          272|       checkUnmatched =
          273|         if config._module.check && config._module.freeformType == null && merged.unmatchedDefns != [] then
             |         ^
          274|           let

       (stack trace truncated; use '--show-trace' to show the full trace)

       error: In module `/nix/store/ilzix4w3ibcf23gcg85nj9z1abnnrzdf-source/hosts/raider/disk.nix', you're trying to define a value of type `string'
       rather than an attribute set for the option
       `meta.disk.filesystem'!

       This usually happens if `meta.disk.filesystem' has option
       definitions inside that are not matched. Please check how to properly define
       this option by e.g. referring to `man 5 configuration.nix'!
```
